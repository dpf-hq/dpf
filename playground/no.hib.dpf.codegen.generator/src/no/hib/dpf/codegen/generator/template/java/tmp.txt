«FOREACH graph.nodes AS n»
				«IF n.getTypeName() == "Package"»
					package «n.name»;
				«ENDIF»
			«ENDFOREACH»
			
«getConstraintForTargetNode(this.target)»
«getConstraintForArrow(this)»


let constrainedNodes = g.constraints.getConstrainedNodes() :
		if constrainedNodes.exists(e|e.id==n.id) then
			let multConstraint = g.constraints.select(e|e.predicate.symbol == "[mult(m,n)]") :
				multConstraint.select(e | e.