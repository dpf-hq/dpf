package no.hib.dpf.codegen.ui.wizards;

import java.net.URI;

public class DPFPlayProjectSupport {
	public static void createProject(String projectName, URI projectLocation, String playPath) {
		/*IProjectDescription description = null;
    try {
      description = ResourcesPlugin.getWorkspace().loadProjectDescription(new Path(PROJECT_PATH + "/.project")); //$NON-NLS-1$ 
      IProject project = ResourcesPlugin.getWorkspace().getRoot().getProject(description.getName());
      project.create(description, null);
      project.open(null);
    } catch (CoreException exception_p) {
      exception_p.printStackTrace();
    }*/
		
	}
	public static void addNature() {
		
	}
}
