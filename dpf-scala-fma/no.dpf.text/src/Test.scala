object Test {
def main(args: Array[String]) {

no.dpf.text.graph.parser.Main.main(Array())
  
//  val x = List(1,2,3,10,11,12,9);
//  for(e<-x.groupBy(_/3)){
//    println(e._1)
//    println(e._2)
//  }
 
  
  /*
	  println("Hello, world!" + (for(c <-Set( 1, 2, 3 )) yield c+1))
      val names = scala.collection.mutable.Map[Int,Int]();
      names+=1->2
      names+=2->3
      val test = names.toMap map {x=>(x._1,x._2)}
      
      
      
      println(test.getClass + " " + test)
      
      val s = Set(1,2,2,3)
      s+4
      println(s.getClass)
 */     
 //     println((names.toMap).getClass + " " + names.toMap)
 //     println(Map(names toSeq: _*).getClass)
 //     println((names map (v => (v._1->v._2))).getClass)

  //println("dfsf\"dfdf".replaceAllLiterally("\"","\\\""))


//tudresden.ocl20.pivot.standalone.example.StandaloneDresdenOCLExample.main(null)
/*
      	val processBuilder = new ProcessBuilder("java","-jar","test.jar")
        processBuilder.redirectErrorStream(true)
        val proc = processBuilder.start()
        val i = new scala.io.BufferedSource(proc.getInputStream())
		for(s<-i)print(s)
*/  
//        val c = <a/> % scala.xml.Attribute(None, "name", scala.xml.Text("value"), scala.xml.Null)
//        println(c)
        
    }
}